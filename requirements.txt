# Core scientific stack
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scipy>=1.11
scikit-learn>=1.3

# Gradient boosting (demand-model diagnostic baseline)
lightgbm>=4.1

# Deep learning
# NOTE (local CPU-only dev): install torch first via
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# to avoid pulling multi-GB CUDA wheels. On Colab torch is preinstalled.
torch>=2.1

# RL environment API
gymnasium>=0.29
pettingzoo>=1.24

# Visualization
matplotlib>=3.7
seaborn>=0.13

# Utilities
pyyaml>=6.0
tqdm>=4.66

# Experiment tracking (CSV is primary; TensorBoard optional live view)
tensorboard>=2.15

# Development / testing
pytest>=7.4
